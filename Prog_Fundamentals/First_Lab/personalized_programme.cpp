#include <iostream>
/* First Lab, Task 2 for C++ Programming 
Fundamentals */

int main()
{
    std::cout << "Hello, my name is Patrick.\n" << std::endl;
    std::cout << "I like pizza. My favourites toppings are ham, mushrooms \
and onions.\n" << std::endl;
    std::cout << "The number two, " << 2 << ", is easily printed. To print \
the symbols # and @ is more difficult because they are reserved symbols in \
the C++ programming language. One cannot just insert them into the input and \
output stream using the insert operator, <<. If the semi-colon is ommited \
at the end of a statement an error message will result upon compiling because \
the semi-colon symbol (;) terminates a statement.\n" << std::endl;

return 0;

}